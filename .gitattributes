* text=auto eol=lf
tests/fixtures/html5lib/** -text
# Preserve the pinned upstream WPT bytes used by the integrity manifest on every host.
tests/fixtures/wpt/** -text
# Preserve raw runner output and historical archive manifests without newline or whitespace rewriting.
reports/validation/runtime-review-fixes/**/*.log -text -whitespace
reports/validation/runtime-review-fixes/isolated-formdata-variant/** -text -whitespace
reports/validation/runtime-review-round2/**/*.log -text -whitespace
reports/validation/runtime-review-round3/**/*.log -text -whitespace
reports/validation/runtime-review-round4/**/*.log -text -whitespace
reports/validation/xml-iterator/**/*.log -text -whitespace
reports/validation/abort-retention/**/*.log -text -whitespace
reports/validation/abort-retention-pr67/**/*.log -text -whitespace
reports/validation/abort-runner-node24/**/*.log -text -whitespace
